/**
 * IR (Intermediate Representation) types for the Kalp compiler.
 *
 * @module
 */

export type {
  IRNodeId,
  IRNodeKind,
  IRNodeBase,
  EntryIRNode,
  HandlerType,
  HandlerIRNode,
  IRNode,
  IREdgeType,
  IREdge,
  IRGraph,
} from "@/ir/types";
