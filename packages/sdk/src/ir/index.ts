/**
 * IR (Intermediate Representation) types for the Kalp compiler.
 *
 * @module
 */

export type {
  IRNodeId,
  IRNodeKind,
  IRNodeBase,
  EntryIRNode,
  HandlerType,
  HandlerIRNode,
  ScheduleIRNode,
  IRNode,
  IREdgeType,
  IREdge,
  AgentMetadata,
  IRGraph,
} from "@/ir/types";
