/**
 * Utility types and helpers for the Kalp SDK.
 *
 * @module
 */

export type { InputOf, OutputOf, Simplify } from "@/utils/types";
export { extractFilePath, captureFilePath } from "@/utils/stack";
